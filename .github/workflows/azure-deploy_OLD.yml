# 🔒 DISABLED WORKFLOW
# This GitHub Action has been archived and will no longer run automatically.
# To re-enable, remove the comment markers below.

# name: Deploy Next.js app to Azure App Service

# on:
#   push:
#     branches:
#       - main
#   workflow_dispatch:

# jobs:
#   build:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: 20
#           cache: 'npm'

#       - name: Install dependencies
#         run: npm ci

#       - name: Build Next.js app
#         run: npm run build

#       - name: Package build for deployment
#         run: zip -r node-app.zip .next public package.json node_modules prisma next.config.mjs

#       - name: Upload artifact
#         uses: actions/upload-artifact@v4
#         with:
#           name: node-app
#           path: node-app.zip

#   deploy:
#     runs-on: ubuntu-latest
#     needs: build
#     steps:
#       - name: Download build artifact
#         uses: actions/download-artifact@v4
#         with:
#           name: node-app

#       - name: Deploy to Azure Web App
#         uses: azure/webapps-deploy@v2
#         with:
#           app-name: mwa-intranet
#           publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
#           package: node-app.zip
